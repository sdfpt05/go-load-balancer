version: '3.8'

services:
  loadbalancer:
    build: 
      context: ..
      dockerfile: deployments/Dockerfile
    ports:
      - "8080:8080"
      - "9090:9090"
    volumes:
      - ../configs:/app/configs
    environment:
      - CONFIG_FILE=/app/configs/config.yaml